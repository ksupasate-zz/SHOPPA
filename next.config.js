/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  images: {
    domains: ['imgur.com', "images.unsplash.com", "static.nike.com" , 'cdn.shopify.com'],
  },
};
